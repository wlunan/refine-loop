# 安全策略

## 支持范围

安全修复优先落在最新发布版本和 `main` 分支。

## 使用边界

RefineLoop 是本地、单用户、受信环境中的代码 Agent 工作台，不是多租户远程执行服务。Agent 会在用户选择的目录中读取文件、创建隔离 Git worktree，并运行受控验证命令。

- 仅对可信代码仓库运行任务。
- 在应用变更前审阅文件清单和 diff。
- 不要将服务直接暴露到公网；当前版本没有认证和租户隔离。
- 不要提交 `.env`、`.task_store` 或模型输出中的凭据。

## 报告漏洞

请通过仓库的 Security Advisory 私下报告漏洞，并附上影响版本、复现步骤和预期影响。请不要在公开 Issue 中粘贴密钥、任务记录或未修复漏洞的完整利用代码。
